

import 'package:flutter/material.dart';

class InfoDomiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Aqui Se Vera El Pedido'),
      )
    );
  }
}